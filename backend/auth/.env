# Server
PORT=3000
ALLOWED_ORIGINS=http://localhost:19006,http://localhost:3000

# SMTP (hMailServer)
SMTP_HOST=127.0.0.1
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=otp@VeloCall.com
SMTP_PASS=Akash@1234das
SMTP_FROM="otp@VeloCall.com"

# Redis (optional) - if empty, in-memory fallback is used (dev only)
REDIS_URL=redis://localhost:6379

# OTP config
OTP_TTL_SECONDS=600
OTP_LENGTH=6
MAX_OTPS_PER_HOUR=5

# Firebase Admin (optional) - path to service account JSON
FIREBASE_SERVICE_ACCOUNT=

# TLS settings for nodemailer (if self-signed certs)
SMTP_REJECT_UNAUTHORIZED=false
